<% provide(:title, "Edit Interval Master " + @interval_master.name) %>

<h1>Edit Interval Master  <%=@interval_master.name %></h1>
TODO!!!!
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@interval_master) do |f| %>
      <%= render 'shared/error_messages' %>

      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' %>

      <%= f.label :email %>
      <%= f.email_field :email, class: 'form-control' %>

      <%= f.submit "Save changes", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
